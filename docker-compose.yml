services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./configs:/app/configs:ro
      - ./input:/input:ro
      - ./output:/output
    environment:
      - TRIARC_WORKSPACE=/app/workspace